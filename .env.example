# Copy this file to .env and fill in real values before running.

# From Neon (neon.tech) — your database connection string
DATABASE_URL=

# Your own admin password
ADMIN_PASSWORD=ats2026

# A long random string — generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-this-secret-in-production

PORT=3000

# --- Contact form email settings (via Resend, not Gmail SMTP) ---
# From resend.com/api-keys after creating a free account
RESEND_API_KEY=

# Where enquiries should be delivered to
CONTACT_TO_EMAIL=josephwasonga40@gmail.com

# Leave this as-is unless you've verified your own domain on Resend
CONTACT_FROM_EMAIL=onboarding@resend.dev
